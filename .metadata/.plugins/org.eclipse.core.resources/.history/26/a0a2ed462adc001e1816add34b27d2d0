package chap02;

public class Greeter {

	private String format;
	
	//bean에서 초기화된 값을 가져와 매개변수를 받아 리턴한다. 
	public String greet(String guest) {
		return String.format(format,  guest);
	}
	
	public void setFormat(String format) {
		this.format=format;
	}
}
